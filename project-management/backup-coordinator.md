# Backup coordinator: ChatGPT/Codex

ChatGPT/Codex can serve as a backup coordinator when Craig invokes it or a configured automation runs. This GitHub connection can read the game repositories and write to this hub; this branch and its pull request are the write-access proof. It is not an always-on process, a live Codex-cloud session monitor, or the exclusive task owner.

Before any takeover, read `COORDINATOR.md`, the tail of `project-management/coordinator-log.md`, both boards, `project-management/workers.md`, and `project-management/rules/SYSTEM.md`. Re-verify every `unverified` worker against its actual platform before claiming or dispatching work. Follow the one-worker-per-task and no-nudge-while-working rules. Record only confirmed transitions in the shared board and append-only log; never place credentials in this public repository.
