# Shared mocks

Art-directed mocks and reference images committed for coding sessions to consume (repo-relative path in briefs; never paste images into session chat). Generation: Muse via the gemini-imagegen skill; sessions use in-session Gemini only as fallback.
