# Shared textures

Commodity textures shared across Craig's game projects (free/CC0 stock only — never AI-generated art passed off as designer-drawn, never commercial assets). Sessions consume files from here by repo-relative path; chat carries only the path.
